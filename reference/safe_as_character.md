# Safely convert to character while preserving NA

Safely convert to character while preserving NA

## Usage

``` r
safe_as_character(x)
```

## Arguments

- x:

  A vector

## Value

Character vector with NA unchanged
